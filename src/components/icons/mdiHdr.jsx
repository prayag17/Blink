/** @format */

import SvgIcon from "@mui/material/SvgIcon";

import { mdiHdr } from "@mdi/js";

export const MdiHdr = (props) => {
	return (
		<SvgIcon {...props}>
			<path d={mdiHdr}></path>
		</SvgIcon>
	);
};
