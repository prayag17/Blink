/** @format */

import { create } from "zustand";

export const useBackdropStore = create((set) => ({
	hash: "LGJCbc|t1s1}1{JU|y,uAo=3,s$Q",
	setHash: (ahash) => set((state) => ({ hash: ahash })),
}));
