/** @format */
import SvgIcon from "@mui/material/SvgIcon";

import { mdiEmoticonDead } from "@mdi/js";

export const MdiEmoticonDead = (props) => {
	return (
		<SvgIcon {...props}>
			<path d={mdiEmoticonDead} />
		</SvgIcon>
	);
};
